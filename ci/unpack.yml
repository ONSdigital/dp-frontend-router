---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: build-bundle
  - name: dp-frontend-router

outputs:
  - name: build

run:
  path: dp-frontend-router/ci/scripts/unpack.sh
